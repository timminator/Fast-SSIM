repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.6
    hooks:
      - id: ruff
        args: [--fix]

  - repo: local
    hooks:
      - id: mypy
        name: mypy
        entry: mypy .
        pass_filenames: false
        language: system
        types: [python]
        args: ["--config-file=pyproject.toml"]
